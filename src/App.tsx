import { FC } from 'react';
import Header from './components/Header';

interface Props {}

const App: FC<Props> = ({}) => {
  return (
    <div>
      <Header />
    </div>
  );
};

export default App;
